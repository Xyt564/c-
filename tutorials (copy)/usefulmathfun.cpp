#include <iostream>
#include <cmath>

// main just a tutorial u can remove the // to test if u wish
int main(){

    // how to find the greater number
    double x = 4.91;
    double y = 4;
    double z;

    // z = std::max(x,y); finds the greater number
    // z = std::min(x, y); finds the min so lowest number
    // z = pow(2, 4); finds the power 
    // z = sqrt(9); finds the square root
    // z = abs(-9); finds how far the number is from zero
    // z = round(x); rounds to the number so e,g 3.14 = 3
    // z = ceil(x); rounds up regardless of number so 4.14 = 5
    // z = floor(x); rounds down so 4.91 = 4
    
    std::cout << z << "\n";

    return 0;
}
