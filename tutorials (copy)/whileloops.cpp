#include <iostream>

// this will continius loop the help im stuck
// use ctrl c to escape it

int main(){

    std::string name;
    
    // while loops just ensure it keeps going until the requirement is met in this case it forces you to enter ur name or it keeps looping
    
    
    // while(name.empty()){
    //    std::cout << "Enter your name: ";
    //    std::getline(std::cin, name);
    // }

    while (1==1){ // infinite loop of text hehehehe 
        std::cout << "HELP! IM STUCK IN A LOOP!" << "\n";
    }
    

    // std::cout << "Hello " << name << "\n";

    return 0;
}
