#include <iostream>

int main(){

    int x; // declaring the variable like naming it pretty much
    
    x = 5; // giving it a number like giving it meaning pretty much
    
    int y = 6; // or combine the two e.g int x = 5;

    int sum = x + y; // adding them up to make one

    std::cout << x << "\n"; // print out the int into text
    std::cout << y << "\n";
    std::cout << sum << "\n"; // prints out the added up answer

    return 0;
}